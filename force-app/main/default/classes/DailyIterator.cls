public with sharing class DailyIterator implements RecurrenceService.RecurrenceIterator {
    public List<DateTime> iterate(DateTime startDate, RecurrenceRule rRule) {
        // Comment
        return new List<DateTime>();
    }
}
